


open(os.path.join(files_path , files_names.format(str(i))) ,'w') 
archivo = 

shutil.rmtree(os.path.join(os.getcwd(), self.directorio))

pat = re.compile("^[^*]")

for line in contents:
    if re.search(pat, line):
        new_contents.append(line)
